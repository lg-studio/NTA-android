<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/select_semester_btn"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">


    <common.views.RoundedImageView
        android:id="@+id/semester_image"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginTop="16dp"
        android:layout_marginLeft="26dp"
        android:layout_centerVertical="true"
        android:src="@mipmap/ic_fake_scene"

        />
    
    <common.views.FontTextView
        android:id="@+id/semester_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginLeft="16dp"
        android:layout_marginTop="16dp"
        android:layout_toRightOf="@+id/semester_image"
        android:layout_centerVertical="true"
        android:textSize="20dp"
        android:text="Semester name"
        app:fontName="@string/roboto_regular"

        />

        <RadioButton
            android:id="@+id/semester_radio_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_marginRight="32dp"
            android:layout_marginTop="16dp"
            android:layout_centerVertical="true"
             />


</RelativeLayout>